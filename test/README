To execute tests for lib:

   rhino -opt -1 run_test.js 

"-opt -1" disables all optimization and exception traces show the call stack

To debug what is happening you can set the logger to "debug" on run_test.js where usually is set performance.
